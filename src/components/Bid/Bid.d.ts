import { IBid } from "../../util/types";

// What gets passed into the component from the parent as attributes
export declare interface IBidInputProps {
    bid?: IBid;
}

export type BidProps = IBidInputProps;