
// Action types
export enum Action {
    InitGame,
    AddPlayer,
    StartGame,
    NextRound,
    SetBid,
    StartRound,
    SetBidStatus
};

// Action creators

// Reducer
export const sevenReducer = (state:any = {}, action:any) => state;

// Selectors
